<h1>Portfolio Items</h1>

<%= link_to "Create New Item", new_portfolio_url %>

<!-- 1. declare the partial's name (it is assumed to be in the portfolios directory but if it was not a full path could be provided)  2. provide the name of the collection that will be used to populate the partial -->
<%= render partial: 'portfolio_item', collection: @portfolio_items %>